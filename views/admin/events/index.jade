extends ../../layout

block title
	title СОБЫТИЯ

block content_style
	link(rel='stylesheet', type='text/css', href='/build/css/add.css')

block content_scripts
	script(type='text/javascript', src='/build/js/lists.js')

block content
	a.back_link(href='/admin') < Назад
	.title СОБЫТИЯ
	br
	a.add(href='/admin/events/add') добавить событие
	br
	br
	.main_menu_block
		each event in events
			.event
				a.rm_event(href='#', id=event._id)= 'удалить '
				!=' | '
				a(href='/admin/events/edit/' + event._id)= event.i18n.title.get('ru')
				!=' --> '
				a(href='/admin/events/edit/' + event._id + '/tickets') билеты